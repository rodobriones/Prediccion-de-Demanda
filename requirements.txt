# Deps de la función Python de Vercel (/api). Mínimo absoluto.
# NO agregar sklearn/scipy/numpy/supabase: no caben en el límite de ~225 MB.
# La inferencia se sirve por lookup del JSON que train.py sube al bucket
# (predicciones deterministas); Supabase se consume por REST vía urllib.
fastapi
pyjwt
