# Deps de la función Python de Vercel (/api). Mínimo absoluto:
# el límite es ~225 MB tras optimizar. No agregar nada más.
# supabase-py se evita a propósito (arrastra ~70 MB); predict.py habla con
# Supabase por REST vía urllib. scipy/numpy entran como deps de scikit-learn.
fastapi
scikit-learn
joblib
pyjwt
